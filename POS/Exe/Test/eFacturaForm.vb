Public Class eFacturaForm

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles btnEnviar.Click
        'Dim gen As New eFactGenerador
        'gen.ListaId = CInt(txtListaId.Text)
        'Dim cant As Integer = CInt(InputBox("Ingrese la cantidad de envios a realizar"))
        'Dim cfcDesde As Integer = CInt(InputBox("Ingrese el numero de CFC desde"))
        'gen.Generar(cant, cfcDesde)

    End Sub

End Class